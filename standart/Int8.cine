rules
    1 = type == Int8
    join 1 BasicSignedInteger

func countAllSet(a Int8) UInt8
    attributes
        alwaysInline
    C
        result_ = __builtin_popcount((uint8_t)a_);

func findFirstSet(a Int8) UInt8
    attributes
        alwaysInline
    C
        result_ = __builtin_ffs(a_);

func toUnsigned(a Int8) UInt8
    attributes
        alwaysInline
    flag debug is set?
        then debugMessage(a < #a, "File: " builtIn_currentFile ", line: " builtIn_currentLine ". UB - assign a negative number to the unsigned variable.")
    C
        result_ = a_;
