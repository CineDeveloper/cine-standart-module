func convertTo(from, to from) to #9
    attributes
        alwaysInline
    result = from

func getTypeSize(a) UInt64
    attributes
        alwaysInline
    C
        result_ = sizeof(##a##);

func getTypeNumber(a) UInt64
    attributes
        alwaysInline
    result = builtInTypeNumber(a)
