group SignedNumber
//require
//abs(a) -> a
//add(a, a) -> a
//dec(:a)
//div(a, a) -> a
//fromString(a, [UInt8]) -> Box(a)
//inc(:a)
//mul(a, a) -> a
//neg(a) -> a
//power(a, a) -> a
//sub(a, a) -> a
//toString(a) -> [UInt8]

rules
    1 = type > SignedNumber
    join 1 Number
