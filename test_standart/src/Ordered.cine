proc testOrdered(:results)
    rules
        final = results == TestResults
    results:test(1.inRange?(1, 1), builtIn_currentFile, builtIn_currentLine)
    results:test(1.inRange?(1, 2), builtIn_currentFile, builtIn_currentLine)
    results:test(1.inRange?(0, 2), builtIn_currentFile, builtIn_currentLine)
    results:test(1.inRange?(0, 1), builtIn_currentFile, builtIn_currentLine)
    results:test(1.inRange?(0, -1), builtIn_currentFile, builtIn_currentLine)
    results:test(!1.inRange?(2, 3), builtIn_currentFile, builtIn_currentLine)
    results:test(!1.inRange?(0, 0), builtIn_currentFile, builtIn_currentLine)
    results:test(1.max(0) == 1, builtIn_currentFile, builtIn_currentLine)
    results:test(1.max(1) == 1, builtIn_currentFile, builtIn_currentLine)
    results:test(1.max(2) == 2, builtIn_currentFile, builtIn_currentLine)
    results:test(1.min(0) == 0, builtIn_currentFile, builtIn_currentLine)
    results:test(1.min(1) == 1, builtIn_currentFile, builtIn_currentLine)
    results:test(1.min(2) == 1, builtIn_currentFile, builtIn_currentLine)
