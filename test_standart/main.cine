proc main()
    results := TestResults
    testAny(results)
    testBasicFloat(results)
    testBasicInteger(results)
    testBasicNumber(results)
    testBasicSignedInteger(results)
    testBasicUnsignedInteger(results)
    testBinary(results)
    testBinaryData(results)
    testBits(results)
    testBool(results)
    testBox(results)
    testBuiltIn(results)
    testChar(results)
    testDouble(results)
    testFloat(results)
    testFluid(results)
    testFunctional(results)
    testInt(results)
    testMap(results)
    testMapTreeNode(results)
    testNumber(results)
    testOS(results)
    testOrdered(results)
    testPair(results)
    testRealNumber(results)
    testSet(results)
    testSlice(results)
    testString(results)
    testTriple(results)
    testUInt(results)
    testVAList(results)
    results.show()
